import React from "react";
import {Container, Grid} from "@mui/material";

const News: React.FC = () => {
  return (
    <Container>
      <Grid item display={"flex"} padding={4}>
        Hello
      </Grid>
    </Container>
  );
};

export default News;